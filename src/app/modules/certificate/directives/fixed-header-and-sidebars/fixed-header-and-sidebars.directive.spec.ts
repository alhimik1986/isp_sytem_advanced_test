import { FixedHeaderAndSidebarsDirective } from './fixed-header-and-sidebars.directive';

describe('FixedHeaderAndSidebarsDirective', () => {
  it('should create an instance', () => {
    //const directive = new FixedHeaderAndSidebarsDirective();
    //expect(directive).toBeTruthy();
  });
});
